# Optional: dedicated Sepolia RPC (Alchemy/Infura/dRPC). Falls back to a public node.
# NEXT_PUBLIC_ values are shipped to the browser, so use a domain/referrer-restricted key.
NEXT_PUBLIC_SEPOLIA_RPC_URL=
